# Toolkit tasks

Project tasks live in `.ai/memory/projects/<slug>/tasks.md`. Toolkit implementation status is tracked in `.ai/backlog.md`.

- [x] Replace sector-specific examples with fictional collaboration scenarios (PR #18).
- [x] Implement and locally validate B24, B22, B28, B26, B27 and B30.
- [x] Provide the B25 recording protocol and validated run format.
- [x] Implement and locally validate B32 to B37 and B39 (grader fix, org layer, eval-design, prototyping ladder, review panel, batch synthesis and connector recipes, pilot runner, human labels).
- [x] Address the owner's review of PR #21 in the same PR: merge `main`, renumber B31 to B39, rewrite the six new assertion blocks with strict fixtures, composite label identity, runner guards, org corrections, proportional eval design, risk-selected panel.
- [x] Address the owner's second review (head `23fd183`): artefact anchors and punctuation variants, stale rubric labels, strict probe with configuration checks and immutable probe files bound to validation, completed Codex turns and per-attempt directories, org bootstrap confinement.
- [x] Address the owner's revalidation of head `ad4f467`: the configuration guarantee runs before any harness call and independently of `--skip-probe`; a `CODEX_HOME` with instructions or MCP servers in `config.toml`, or any entry the session would load, is refused; `-c`/`--config` overrides are refused.
- [ ] Execute the real B25 pilot with `scripts/run_eval_pilot.py` on a machine where both CLIs are authenticated, one iteration per harness; a smoke `--eval` run first, then list the harness version in `verified_harness_versions`.
- [ ] Label the recorded outputs with `scripts/label_eval_run.py` and publish `docs/benchmarks/<iteration>/report.md` per harness.
- [x] Integrate the consolidated implementation after CI validation: PR #19 merged as `7650b2d` and PR #20 as `474ce87` (2026-09-10), PR #21 as `4929635` and PR #22 as `b51ddd3` (2026-09-14), PR #23 as `44b7e51`, PR #24 as `efcba28` and PR #25 as `1487044` (2026-09-16); rows in `docs/PR_HISTORY.md`. Nothing from this cycle is left open.
- [ ] B38 candidate for a next cycle: positioning/GTM, build-vs-buy and win/loss references.
- [x] B40 (separate PR, 2026-09-14): every graded eval's assertion block checks relations, not terms; strict pairs (keyword-only in five joins, near miss, label soup) for 42 of 42 graded evals; 13 in-code fixtures moved to the JSON.
- [x] B41 (separate PR, 2026-09-14): every standard eval's assertion block checks relations, not terms; strict pairs for 43 of 43 standard evals (85 of 85 in total); the test requires a strict pair for every eval; B12, B18 and B20 in-code fixtures moved to the JSON.
- [x] Address the owner's review of PR #23 (head `f5523d2`): the grader reads every assertion through a soft-wrap normaliser and every good fixture re-wrapped at 72 columns stays in band; `validate-skill-repo-health` accepts the honest zero on the strength of the checks and needs one remedy per real failure, with zero- and one-finding controls. Second round (2026-09-15): the normaliser decides each break from the two lines it joins, so an unwrapped paragraph elsewhere no longer blocks the rejoin, enumerated labels stay boundaries, and each failure needs its own remedy before the next failure or a remedy that names both. Third round (2026-09-16): a sentence that states a new failure with its own remedy no longer settles an earlier open failure; both punctuation forms are in-code near misses.
- [x] B42 (separate PR, 2026-09-16): labelled grader disagreements become review proposals under `docs/benchmarks/<iteration>/proposals/` and a pasted candidate is re-checked step by step, with nothing in the loop writing to a manifest, an assertion block or the fixtures file; product golden sets get `scripts/golden_set.py` for the paths `eval-design.md` already published. An adversarial review before the push found six defects in the proposal pass, all fixed here with regressions: a false-accept card that claimed every assertion passed at a partial pass rate, a filtered pass that orphaned the cards it left on disk, a corrupt grading record and a label line without `supersedes` that each aborted the whole pass, an excerpt fence a model's own fence could close, and a card that kept recommending a change after the labels stopped disagreeing. Twenty-six further candidates were triaged by hand, because the verification stage stopped at a spend limit.
- [x] B43 (separate PR, 2026-09-16): the README answers what the toolkit does and what to type before it explains itself, the counts in it are derived from the tree by `check_readme_contract` rather than typed, and `CONTRIBUTING.md`, `SECURITY.md` and a pull request template exist for the first time.
- [ ] B45 candidate (was the unnamed "B42 candidate"): teach `memory.py doctor` the project `evals/` tree, structural only and warnings only; the content rules stay in `golden_set.py check` so they live in one place.
- [ ] B44 candidate: a validator check comparing the README hook table's "Fires on" column against `hooks/contract.json`, per harness.
- [ ] B46 candidate, blocked on B25 evidence: hardening and release readiness (grader-disagreement fixes, `humanize-gate` per-tool extraction, Codex project-isolation coverage, Python 3.12/3.13 and macOS, ShellCheck, third-party notices, a `v0.1.0` tag). Nothing starts before the pilot reports.
- [ ] Owner-only, on GitHub: protect `main` (require a pull request, require the aggregate `validate` status, block force pushes and branch deletion; do not require independent approval while the repo has one active maintainer) and enable private vulnerability reporting under Settings → Code security, which `SECURITY.md` points at. Record an explicit deferral instead if either is declined.
