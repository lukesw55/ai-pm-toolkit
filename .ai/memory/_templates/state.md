# State — <Project Name>

> Newest first. One `## <Parked|Active|Closed> <date>` block per park/close event, written by `scripts/memory.py park`. Caps: pointer (`active-context.md`) ≤2 KB total; this file has no cap but is read only when resuming THIS project; `changelog.md` keeps the 3 most recent entries (older → `changelog-archive.md`).

## <Active|Parked|Closed> YYYY-MM-DD

- **Current stage**: <discovery|define|develop|delivery>
- What happened this session / where things stand
- Open threads on resume (ordered)
- Key artefacts (relative links)
