# State — {{title}}

> Newest first. One `## <Parked|Active|Closed> <date>` block per park/close event, written by `scripts/memory.py park`. Caps: pointer (`active-context.md`) ≤2 KB total; this file has a 16 KB soft cap (`memory.py doctor` warns above it; `memory.py distill --prepare --file state` folds the oldest blocks into `state-archive.md`); `changelog.md` keeps the 3 most recent entries (older → `changelog-archive.md`).
