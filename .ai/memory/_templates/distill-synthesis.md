## {{date}} — Distilled: {{count}} block(s) folded from `{{source}}`

[Fill in: three to six lines. Keep the decisions, the constraints that still bind, and the open threads; drop the narration. Every line should trace back to one of the blocks listed below.]

Folded blocks (verbatim in `{{archive}}`):
{{index}}
