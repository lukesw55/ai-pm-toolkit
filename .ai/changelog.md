# Changelog

<!-- Toolkit/repo changes (skills, hooks, doctrine, scripts). Project work logs live in each project's memory under .ai/memory/projects/<slug>/changelog.md. Append via: python3 scripts/memory.py log repo "<entry>". -->

> Active log keeps the most recent entries; older entries in `changelog-archive.md`.

## 2026-09-16: B43 review 4: the contributor battery runs on a clean clone

Reproduced: a checkout of the tracked tree ships .ai/memory with only README.md, _templates and active-context.example.md, because real memory is gitignored, so memory.py doctor exits 1 with "active-context.md missing". That was the last line of the block, under a sentence claiming CI runs the same commands. CI does not: it runs init_context.py "CI Bootstrap Demo" first, with a comment in the workflow saying exactly why. The block also omitted the pilot dry-run that both CI and docs/REPO_HEALTH.md carry. I took the second option you offered. The displayed block is now the stateless subset, with the pilot dry-run added and the doctor removed, and the intro no longer claims CI runs it verbatim; it says these commands run on a clean clone in any state, which I checked by running every one of them in a fresh archive of HEAD. All nineteen pass there, the dry-run included. The doctor gets its own paragraph rather than a footnote, because a contributor should know why it is missing and where it went: it belongs to the bootstrap smoke test in docs/REPO_HEALTH.md, CI bootstraps a throwaway project before calling it, and nothing is lost by leaving it out of the block, since check_memory_bootstrap already runs init_context.py, --org, the doctor and stage_context.py against a copy of the tree on every validate_repo.py run. The contract is checked; the contributor's own memory is not written to. Full docs/REPO_HEALTH.md battery green before the commit.

## 2026-09-16: B43 review 4: the eval counts come from the canonical skill set

You are right on all four points, and they compound: the helper I added last round fixed the traceback for the shapes it saw and left the discovery itself wrong. eval_categories globbed skills/*/evals/evals.json while the rest of the validator, check_eval_coverage included, defines a canonical skill as skills/*/SKILL.md. I ran the four divergences. An orphan manifest under a directory with no SKILL.md added 3 cases to the README totals and produced two phantom findings, while check_eval_coverage never visited it, so nothing reported the directory either. A canonical skill shipping no manifest was never visited at all, so completeness stayed true and the README was compared against a total taken from an incomplete set. A category that is a string but outside EVAL_CATEGORIES was counted into the eval-case total while missing from the four named counts, producing a count finding on top of the schema finding that check_eval_coverage already raises for it. And read_text with encoding utf-8 raises UnicodeDecodeError, which is a ValueError: neither the OSError nor the JSONDecodeError clause caught it, so invalid UTF-8 still killed the run with a traceback before the reporting check ever ran. The counting now walks skills/*/SKILL.md and resolves each expected evals/evals.json, so orphan directories cannot reach the totals and a missing manifest lowers completeness like any other unusable one. The except clause is deliberately wide, with the reason written next to it: this helper produces no finding of its own, so swallowing costs nothing, and raising costs the finding check_eval_coverage exists to produce. A category outside the taxonomy is skipped and lowers completeness rather than being counted, which keeps the schema finding single. Your four acceptance regressions are in the suite, and each one is red against the previous implementation: the orphan gave two findings and now gives none; the skill with no manifest had its stated 99 compared and now skips the manifest-derived counts; the invalid category gave a category finding and now gives none; the non-UTF-8 file raised UnicodeDecodeError and is now skipped. 81 cases in the file. On the real tree completeness is still true and the counts are still compared, at 21 skills, 4 blocking hooks, 10 agents, 85 eval cases and 43/11/15/16. The REPO_HEALTH clause says the derivation is from the canonical skill set now, not just that it is defensive. Full docs/REPO_HEALTH.md battery green before the commit.

## 2026-09-16: B43 review 3: the hook contract is per harness, and Codex trust covers its own adapter file

Two README claims from the same review. The wiring block said test_hook_contract.py asserts that both adapters route the same events to the same scripts. It does not, and the contract does not ask for that. hooks/contract.json declares a route set per harness, 33 for Claude Code and 30 for Codex, with an exceptions field that records in words that the project-isolation warning is not wired on the Codex side; apply_patch reaches the shared write gates through .codex/adapters/pretooluse.py rather than through an identical route. test_real_contract calls check_hook_contract and requires zero errors, which is each adapter matching its own declared set. The paragraph now says that, and names the two differences rather than papering over them. The trust instruction said to re-run /hooks after editing hooks/*.sh. The adapter's own comment says to re-run it after editing hooks/*.sh or .codex/hooks.json, because Codex tracks trust by content hash and the adapter file is part of what it hashes. You cited the quick start and the troubleshooting entry; the wiring table's Trust step row had the same omission, so all three say it now. Counts untouched, so check_readme_contract stays green. Full docs/REPO_HEALTH.md battery green before the commit.

