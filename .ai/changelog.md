# Changelog

<!-- Toolkit/repo changes (skills, hooks, doctrine, scripts). Project work logs live in each project's memory under .ai/memory/projects/<slug>/changelog.md. Append via: python3 scripts/memory.py log repo "<entry>". -->

> Active log keeps the most recent entries; older entries in `changelog-archive.md`.

## 2026-09-16: B42 review: the decision record carried the sentence the rest of the round corrected

The review round fixed the direction a good fixture candidate comes from in the protocol, in the repo-health checklist, in the script's docstring and in the card's own prose, and missed the line in docs/DECISIONS.md that says the same thing. It still read that the candidate comes from a run the grader already accepts. It does not: a good candidate exists only for a false reject, which is a run the grader rejected and a human accepted. The reason to rewrite it by hand is that it is the model's own output, not that the grader agreed with it. One sentence, in the file that is meant to be the durable version of the rule, so it is the one place where leaving it wrong would outlive the PR. The changelog entries that quote the old wording stay as they are: they record what was written at the time, and the entry for the correction is the record that it changed. Full docs/REPO_HEALTH.md battery green before the commit.

## 2026-09-16: B42 review: the docs and the suite say what the code does

The last group from the review: seven places where a sentence or a test claimed more than the code delivered. Two are fixed by making the code true, five by correcting the sentence. The protocol and REPO_HEALTH said the suite asserts all three protected paths stay byte-identical after a pass. It compared two: scripts/grade_evals.py was never in the sandbox to compare. It is copied in now and checked with the other two, so the claim holds rather than shrinks. Both documents also said agreement in either direction proposes nothing, with only the positive direction tested. The negative one, where the assertions reject the run and a human failed it too, is a case now. The new validator cases asserted the GENERATED_RECORDS regex and nothing else, so neither document check was ever shown to consult it. Two cases run check_markdown_links and check_backtick_paths over a temporary tree holding the same broken document twice, once as a proposal and once as the report.md beside it, and require exactly one error naming the report. Removing the skip fails them. The corrected sentences. An eval-defect handle does not outrank the others: a stale rubric and a split are read before it, and a split carrying that handle yields no card at all. A `good` candidate does not come from a run the grader already accepts; it comes from a false reject, which is a run the grader rejected and a human accepted, and the reason to rewrite it by hand is that it is the model's own output, not that the grader agreed. The check mode does not report eight fixtures for a pair; it reports every fixture the suite ran for that eval, which for the eval the tests exercise is fifteen, the eight derived from the pair and seven written in code. And the comment on the validator skip said reports and label files in the same tree stay checked, when both checks read markdown only, so a labels.jsonl was never in scope for either. Two smaller ones in the check mode itself, from the same review. A near_miss whose text or fails is not a string passed the shape step, because the emptiness test stringified it first, and the crash that followed was reported as a checkout that predates --json-summary. And the suite's stderr was printed only when its stdout was empty, so a traceback was never the thing you saw. Both streams are printed now, labelled. 35 cases in the proposer, 48 in the golden set, 61 in the validator. Full docs/REPO_HEALTH.md battery green before the commit.

## 2026-09-16: B42 review: golden_set reads what a spreadsheet writes, and checks what the flags skipped

The same review went through golden_set.py. Nine findings, all small, one of them the kind that makes a tool useless on first contact. A sheet saved from a spreadsheet as "CSV UTF-8" carries a byte-order mark, so the first column read as something other than id and every subcommand refused the file with a message about the header. The docstring says a PM opens this in a spreadsheet, which makes that the normal path, not an edge case. The sheet is read as utf-8-sig now; the rewrite stays utf-8, so the mark does not come back. Confinement stopped at the directory. context_paths confines <slug>/evals/<feature>, but the file name was joined on afterwards, so a symlink at <feature>/golden-set.csv pointing out of the project was followed by every read and by init's write, which reported an in-project path while writing somewhere else. Same class as the init_context --org finding from the third review round of PR #21, same fix: the leaf goes through project_path too. write_sheet replaced the sheet with a temp file, and os.replace keeps the temp file's mode, so every add silently narrowed 0644 to 0600. The mode is carried over now. Line numbers in the messages were counted rather than read, so one blank line or one quoted field spanning lines shifted every number after it. They come from the reader now and name the row's last physical line. Those messages exist to say where to look. --id skipped every validation the other fields get: it could be empty, span lines, or be non-numeric, and a non-numeric id disables the automatic id for every later add without saying so. It is validated like the rest now, with a warning rather than a refusal for the last case, because the error that suggests --id in the first place allows it. --stale-after was never parsed as a date, only compared as text, so a typo like 2026-1-1 sorted after every ISO date and flagged every row stale, which with --strict is an exit code. It is parsed before the sheet is read, and the comparison is between dates. check accepted an empty input locator and an empty expected behaviour, then reported two empty locators as duplicates of each other. Both are errors now and the duplicate scan skips them. It also warns when a cell starts with =, + or @, which a spreadsheet evaluates as a formula rather than showing; a leading minus is left out on purpose, because in this data it is a negative number or a dash far more often than a formula. Last, next_id guarded with isdigit() where int() is narrower, so an id like a superscript digit reached int() and produced the wrong message. isdecimal() is the guard that matches. Twelve regression cases, 48 in the file. Full docs/REPO_HEALTH.md battery green before the commit.

