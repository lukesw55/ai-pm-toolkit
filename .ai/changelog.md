# Changelog

<!-- Toolkit/repo changes (skills, hooks, doctrine, scripts). Project work logs live in each project's memory under .ai/memory/projects/<slug>/changelog.md. Append via: python3 scripts/memory.py log repo "<entry>". -->

> Active log keeps the most recent entries; older entries in `changelog-archive.md`.

## 2026-09-16: Merge the B42 branch: the three review fixes

You asked for the final #24 fixes to be in this branch before the final validation, so the merge brings across the false-accept remediation target, the list confinement and the withheld model text. Conflicts in three files, all of them places both branches appended to. docs/DECISIONS.md keeps the B42 row as the other branch corrected it and the B43 row this branch added. The two changelogs keep every entry in chronological order, with the three most recent in the active log and the rest in the archive, whose index was rebuilt with memory.py index repo. The active log sits a little over its soft cap with three long entries; the next entry rotates the oldest out, which is what the cap is for. Full docs/REPO_HEALTH.md battery green before the commit.

## 2026-09-16: B43 review: the README says which layer each part of the loop belongs to

Four claims from your review, all of them checked against .claude/settings.json and .codex/hooks.json, which wire the same events to the same scripts. Memory and stage were described as one UserPromptSubmit injection. They are two: SessionStart runs hooks/memory-context.sh for the memory pointer, UserPromptSubmit runs scripts/stage_context.py for the current stage. Two sentences now, one per event, naming the script. "Enforce at tool-call time" was true of three gates and not of the fourth. anti-slop, inference-discipline and humanize run on PreToolUse; scope-bloat-gate.sh runs on Stop, on the reply itself, which is not a tool call. The slogan and the lede now say both, and the diagram nodes name the events instead of merging them. The lede said the gates reject unverified claims. The hook table three sections below it already gave the real contract: inference-discipline-gate.sh matches five literal unresolved markers, and semantic fact-checking is the skill's job. The lede now says unresolved inference markers. That contradiction is the same shape as the one in the proposal card this batch fixed on the other branch, where the top of a document said something the table further down disproved, and it is worth naming as a pattern rather than a one-off. "Nothing in that loop is advisory" replaced by the three layers you proposed, because the sentence flattened things that behave differently. Enforced by the harness: the four blocking gates, named with their events. Injected context: the memory pointer at SessionStart and the stage on every prompt, which shape what the model sees rather than what it may do. Agent behaviour: which skill runs, what the artefact says, whether memory is updated, with memory-reminder.sh nudging at Stop by design rather than blocking. The last line of that section says the artefact and the changelog entry are files you can diff, which is how a reader checks the third layer instead of trusting it. The counts are untouched, so check_readme_contract stays green; the phrase it validates for the gates is still literal. Full docs/REPO_HEALTH.md battery green before the commit.

## 2026-09-16: B42 review: no model text in a tracked file unless someone asks for it

Your third finding holds, and it is wider than the excerpt you named. Two things carried the recorded output into docs/, not one. The excerpt copied up to 1,200 characters into the tracked markdown. The fixture candidate copied the whole output, because the candidate is that output, and it went into both the markdown and the JSON sibling. And record_eval_run.py records whatever output its caller passes it, so nothing in a card proved the text came from the isolated pilot. The default now carries no model text at all. The excerpt is empty with embedded false, and the candidate slot holds a placeholder naming the path where the text lives. Everything the decision actually rests on stays: the output hash, the run path, how many characters it is, the grader table, the labeler's reason, and for the candidate the slot, the band, the sha256 of the value it would replace, the collision warning and the rewrite mark. Those are all computed from the real text in memory; only the publishing stops. --embed-output writes the bounded excerpt and the candidate text in. --full-output implies it and drops the bound. Both are a person's decision, which is the same shape as the rest of this loop. The provenance half of your suggestion is in as a signal rather than a second flag: run.provenance_present records whether the pilot sidecar sits beside the run, the card says so in the output section, and embedding a run without one warns on stderr. A reviewer who embeds can see whether the text came from the isolated pilot or from a hand-recorded file. Proof, run against a card generated by the default path with a canary string in the recorded output: neither the .md nor the .json contains any part of it, while both carry the hash and the run path. Two regressions pin it, one for the default and one for --embed-output, and the three earlier cases that needed the text embedded now ask for it. 40 cases in the file. Documentation followed the default: the protocol paragraph, the repo-health line, the backlog entry and the decision record all said bounded where they now say withheld. Full docs/REPO_HEALTH.md battery green before the commit.

